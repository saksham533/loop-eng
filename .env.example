# Ports
ORDER_PORT=4001
BILLING_PORT=4002
ERP_PORT=4003

# Service URLs (used by each mock to talk to its peers)
ORDER_URL=http://localhost:4001
BILLING_URL=http://localhost:4002
ERP_URL=http://localhost:4003

# Simulation flags
SIMULATE_FIRST_CRASH=true
IDEMPOTENCY_STRATEGY=naive